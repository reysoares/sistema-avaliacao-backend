spring.application.name=avaliacao

# Habilita o console web do H2
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Configuração do H2 (banco em memória)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=test

# Hibernate: cria/tenta atualizar tabelas automaticamente
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Caminho onde as imagens ficarão salvas localmente
project.image=images/

# ---- PostgreSQL comentado para uso posterior ----
# spring.datasource.url=jdbc:postgresql://postgres:5432/polls
# spring.datasource.username=docker
# spring.datasource.password=docker
